#!/bin/bash
if [ "$#" -lt 1 ]; then
    echo "Usage: $0 VAR_FILE [OPTIONS...]"
    echo "where VAR_FILE is ubuntu-server-20.04.json, xubuntu-20.04.json, ..."
    exit 1
fi

packer build -timestamp-ui -var-file=$* packer/template.json
